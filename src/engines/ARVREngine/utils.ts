// src/engines/ARVREngine/utils.ts
export function normalizeARData(rawData: any) {
    // Converts raw AR input to standardized format
    return { normalized: rawData };
}

export function normalizeVRData(rawData: any) {
    // Converts raw VR input to standardized format
    return { normalized: rawData };
}
