export async function runInSandbox(action: string, folder: string) {
    // Placeholder for executing safe actions
    console.log(`[Sandbox] Executing ${action} on ${folder}`);
    return { actionExecuted: action, folder };
}
